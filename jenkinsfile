pipeline {
    agent any
    stages {
        stage('build') {
            mvn package
        }
        stage('test') {
            echo "test...."
        }
        stage('deploy') {
            echo "deploy ..."
        }
    }
}
