
pipeline {
	agent any
	
	stages {
		stage('Build'){
			steps {
				echo 'Build ...'
				echo "${env.WORKSPACE}/target/*.jar"
			}
		}
		stage('Test') {
			steps {
				echo 'Testing...'
			}
		}
		stage('Deploy') {
			steps {
				echo 'Deploying...'
			}
		}
	
	}
}
